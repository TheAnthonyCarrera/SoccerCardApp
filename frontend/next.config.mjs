/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'images.unsplash.com',
        'www.investopedia.com',
        'upload.wikimedia.org'
    ],
    },
  };
  
  export default nextConfig;