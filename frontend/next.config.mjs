/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'images.unsplash.com',
        'www.investopedia.com'
    ],
    },
  };
  
  export default nextConfig;